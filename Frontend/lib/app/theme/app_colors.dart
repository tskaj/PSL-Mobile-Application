import 'package:flutter/material.dart';

class AppColors {
  // Gradient brand
  static const Color blue = Color(0xFF2563EB);
  static const Color purple = Color(0xFF9333EA);

  // Page background (pure white)
  static const Color bg = Colors.white;

  // Card
  static const Color card = Colors.white;
  static const Color cardBorder = Color.fromARGB(255, 218, 228, 253); // blue border
  static const Color cardShadow = Color.fromARGB(26, 33, 90, 214);  
  // Text
  static const Color text = Color(0xFF0F172A);
  static const Color textMuted = Color(0xFF64748B);

  // Shadow (light)
  static const Color shadow = Color.fromARGB(19, 0, 0, 0); // very light

  // Input border
  static const Color border = Color.fromARGB(255, 212, 220, 231);
}
